plugins {
//    id "com.moowork.node" version "1.3.1"
    id "com.github.node-gradle.node" version "2.2.4"
    id 'base'
}
node {
//    npmInstallCommand = 'ci'
	download = true
	version = '14.17.0'
}

clean {
    delete 'node_modules'
}

assemble.dependsOn('check', 'npm_run_build')
